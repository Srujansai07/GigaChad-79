/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    // Enable if using images from external sources
    // images: {
    //   domains: ['example.com'],
    // },
};

module.exports = nextConfig;
